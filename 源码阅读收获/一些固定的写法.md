# 边遍历，边删除符合条件的.
```java
Iterator<RecommendAd> iterator = recommendAds.iterator();
                    while (iterator.hasNext()) {
                        if (xxx) {
                            iterator.remove();
                        }
                    }
```
